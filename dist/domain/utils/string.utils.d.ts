export declare const STRING_UTILS: {
    random: (length: number) => string;
    compare: {
        lowerCase: (a: string, b: string) => boolean;
        exact: (a: string, b: string) => boolean;
    };
};
